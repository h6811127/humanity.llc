# Cloudflare Pages  -  static site in /site
#
# Dashboard (Git):
#   Build command:        empty or `npm run build`
#   Build output dir:     site  (also set below for wrangler)
#   Deploy command:       LEAVE EMPTY  -  do not use `npx wrangler deploy`
#
# CLI: npm run deploy  →  wrangler pages deploy site

name = "humanity-llc"
compatibility_date = "2024-08-01"
account_id = "6ad770a7c6336c0c13cfb2c88c2bf8da"
pages_build_output_dir = "site"
