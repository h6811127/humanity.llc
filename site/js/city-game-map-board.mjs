/**
 * @deprecated Import city-game-play-page.mjs on play pages (single boot).
 * Kept so old HTML cache still loads the full play surface.
 */
import { bootCityGamePlayPage } from "./city-game-play-page-core.mjs";

bootCityGamePlayPage();
