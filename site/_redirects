/assets/glitch-print-chromatic-unit-example.png  /assets/glitch-print-chromatic-artifact.png  200
# Do not rewrite /create → create.html (Pages 308-strips .html and causes a redirect loop).
# Unknown paths: site/404.html (HTTP 404). Disable SPA fallback in Pages project settings.

/create  /create/  301
/objects  /wallet/  301
/objects/  /wallet/  301
# Product detail SPA — rewrite outside /shop/products/* (Pages 308-strips .html; a target like
# /shop/products/detail.html becomes /shop/products/detail, which re-matches the splat and loops).
/shop/products/*  /shop/product-detail/  200
# DISCOVER_PIN_SPLAT_BEGIN
# Discovery pin share URLs — one splat per region (double splat does not work on Pages).
# Regenerated by npm run discover:rebuild-pins from site/data/discovery-*.json
/discover/cedar-rapids-iowa/pin/*  /discover/pin/  200
# DISCOVER_PIN_SPLAT_END
