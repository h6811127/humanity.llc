/**
 * Superseded by e2e/hub-custody-row-mobile-layout.spec.ts (mobile stacked custody rows).
 * Kept as a thin redirect so old npm scripts / bookmarks fail loudly with the new path.
 */
import { test } from "@playwright/test";

test.describe("Phase 0 hub custody unsaved layout", () => {
  test.skip(true, "Use e2e/hub-custody-row-mobile-layout.spec.ts");
});
